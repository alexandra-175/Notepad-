<Window x:Class="notepad__.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:notepad__"
        Title="Notepad++ Clone" Height="650" Width="1000">

    <Window.Resources>
        <local:DirToIconConverter x:Key="DirToIconConverter"/>
        <ContextMenu x:Key="TreeItemContextMenu">
            <MenuItem Header="New file" Click="NewFileFromTree_Click"/>
            <MenuItem Header="Copy path" Click="CopyPath_Click"/>
            <MenuItem Header="Copy folder" Click="CopyFolder_Click"/>
            <MenuItem Header="Paste folder" Click="PasteFolder_Click"/>
        </ContextMenu>
    </Window.Resources>

    <DockPanel>
        <!-- MENIU -->
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="File">
                <MenuItem Header="New file" Command="{Binding NewFileCommand}"/>
                <MenuItem Header="Open file..." Command="{Binding OpenFileCommand}"/>
                <MenuItem Header="Save file" Command="{Binding SaveCommand}"/>
                <MenuItem Header="Save file as..." Command="{Binding SaveAsCommand}"/>
                <MenuItem Header="Close all files" Command="{Binding CloseAllCommand}"/>
                <Separator/>
                <MenuItem Header="Exit" Click="Exit_Click"/>
            </MenuItem>

            <MenuItem Header="Search">
                <MenuItem Header="Find..." Click="OpenSearch_Click"/>
                <MenuItem Header="Replace..." Click="OpenReplace_Click"/>
                <MenuItem Header="Replace All..." Click="OpenReplaceAll_Click"/>
                <Separator/>
                <MenuItem x:Name="SelectedTabMenu" Header="Selected tab" IsCheckable="True" IsChecked="True" Click="SelectedTab_Click"/>
                <MenuItem x:Name="AllTabsMenu" Header="All tabs" IsCheckable="True" Click="AllTabs_Click"/>
            </MenuItem>

            <MenuItem Header="View">
                <MenuItem Header="Standard" Click="StandardView_Click"/>
                <MenuItem x:Name="ViewExplorer" Header="Folder Explorer" IsCheckable="True" IsChecked="True" Click="ViewExplorer_Click"/>
            </MenuItem>

            <MenuItem Header="Help">
                <MenuItem Header="About" Click="About_Click"/>
            </MenuItem>
        </Menu>

        <!-- TOOLBAR -->
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Content="📄 New" Command="{Binding NewFileCommand}" Margin="2"/>
                <Button Content="📂 Open" Command="{Binding OpenFileCommand}" Margin="2"/>
                <Button Content="💾 Save" Command="{Binding SaveCommand}" Margin="2"/>
            </ToolBar>
        </ToolBarTray>

        <!-- GRID PRINCIPAL -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ExplorerColumn" Width="250"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Folder Explorer -->
            <GroupBox Header="Folder Explorer" Grid.Column="0">
                <TreeView x:Name="MyTreeView" ItemsSource="{Binding DirectoryTree}">
                    <TreeView.ItemContainerStyle>
                        <Style TargetType="TreeViewItem">
                            <EventSetter Event="Expanded" Handler="TreeView_Expanded"/>
                            <EventSetter Event="MouseDoubleClick" Handler="TreeViewItem_MouseDoubleClick"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding IsDirectory}" Value="True">
                                    <Setter Property="ContextMenu" Value="{StaticResource TreeItemContextMenu}"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TreeView.ItemContainerStyle>
                    <TreeView.ItemTemplate>
                        <HierarchicalDataTemplate ItemsSource="{Binding Items}">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{Binding IsDirectory, Converter={StaticResource DirToIconConverter}}" Margin="0,0,5,0"/>
                                <TextBlock Text="{Binding Name}"/>
                            </StackPanel>
                        </HierarchicalDataTemplate>
                    </TreeView.ItemTemplate>
                </TreeView>
            </GroupBox>

            <!-- Separator (opțional, poate fi un GridSplitter) -->
            <GridSplitter Grid.Column="1" Width="5" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>

            <!-- TabControl -->
            <TabControl Grid.Column="2" ItemsSource="{Binding Tabs}" SelectedItem="{Binding SelectedTab}">
                <TabControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding DisplayName}" VerticalAlignment="Center"/>
                            <Button Content="x"
                                    Command="{Binding DataContext.CloseTabCommand, RelativeSource={RelativeSource AncestorType=TabControl}}"
                                    CommandParameter="{Binding}"
                                    Margin="5,0,0,0" Padding="4,0" Background="Transparent" BorderThickness="0"/>
                        </StackPanel>
                    </DataTemplate>
                </TabControl.ItemTemplate>
                <TabControl.ContentTemplate>
                    <DataTemplate>
                        <TextBox Text="{Binding Content, UpdateSourceTrigger=PropertyChanged}"
                                 AcceptsReturn="True" AcceptsTab="True"
                                 FontFamily="Consolas" FontSize="14"
                                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"/>
                    </DataTemplate>
                </TabControl.ContentTemplate>
            </TabControl>
        </Grid>
    </DockPanel>
</Window>